import { ProjectWorkflowService } from './project-workflow.service';
describe('ProjectWorkflowService payment commands',()=>{const project:any={project_id:'project',status:'awaiting_deposit'};it('converts dollars to cents and returns database warnings without client-side status writes',async()=>{const rpc=jasmine.createSpy().and.resolveTo({data:{state:'duplicate_warning',suspectedReference:'BBP-1'},error:null});const repository:any={client:{rpc},getProjectById:jasmine.createSpy().and.resolveTo(project)};const service=new ProjectWorkflowService(repository,{} as any);const result=await service.recordPayment(project,{obligation_id:'deposit',payment_kind:'deposit',amount:12.34,received_at:'2026-01-01T12:00:00Z',payment_method:'cash',command_key:'command'});expect(rpc).toHaveBeenCalledWith('record_manual_payment',jasmine.objectContaining({p_amount_cents:1234,p_command_key:'command'}));expect(result.result.state).toBe('duplicate_warning');});it('validates required positive receipt evidence',async()=>{const service=new ProjectWorkflowService({} as any,{} as any);await expectAsync(service.recordPayment(project,{obligation_id:'',payment_kind:'deposit',amount:0,received_at:'',payment_method:null as any})).toBeRejected();});});
