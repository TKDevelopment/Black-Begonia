import { TestBed } from '@angular/core/testing';
import { ActivatedRoute } from '@angular/router';
import { CustomerPaymentService } from '../../../core/supabase/services/customer-payment.service';
import { PaymentStatusComponent } from './payment-status.component';
describe('PaymentStatusComponent',()=>{it('ignores browser return wording and renders only the polled server state in a live region',async()=>{const payment={poll:jasmine.createSpy().and.resolveTo({state:'still_outstanding'}),resolve:jasmine.createSpy()} as any;await TestBed.configureTestingModule({imports:[PaymentStatusComponent],providers:[{provide:CustomerPaymentService,useValue:payment},{provide:ActivatedRoute,useValue:{snapshot:{paramMap:{get:()=> 'token'},queryParamMap:{get:(key:string)=>key==='attempt'?'attempt':key==='status'?'confirmed':null}}}}]}).compileComponents();const fixture=TestBed.createComponent(PaymentStatusComponent);await fixture.componentInstance.ngOnInit();fixture.detectChanges();expect(fixture.nativeElement.textContent).toContain('Balance still outstanding');expect(fixture.nativeElement.textContent).not.toContain('Payment confirmed');expect(fixture.nativeElement.querySelector('section[aria-live="polite"]')).not.toBeNull();expect(payment.poll).toHaveBeenCalledWith('token','attempt');});});
