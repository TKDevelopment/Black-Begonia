import { TestBed } from '@angular/core/testing';import { PaymentRepositoryService } from '../../../core/supabase/repositories/payment-repository.service';import { PaymentsComponent } from './payments.component';
describe('PaymentsComponent',()=>{let repository:jasmine.SpyObj<PaymentRepositoryService>;beforeEach(async()=>{repository=jasmine.createSpyObj('PaymentRepositoryService',['listObligations']);repository.listObligations.and.resolveTo({rows:[{project_payment_record_id:'deposit',project_id:'project',project_name:'Avery Event',customer_name:'Avery',customer_email:'a@example.com',payment_kind:'deposit',status:'partially_paid',amount_due:300,amount_paid:100,target_amount:300,credited_principal:100,outstanding_amount:200,payment_source:'manual',last_method:'check',has_exception:false,has_delivery_issue:false,created_at:'2026-01-01',updated_at:'2026-01-01'}],total:1,page:1,pageSize:25});await TestBed.configureTestingModule({imports:[PaymentsComponent],providers:[{provide:PaymentRepositoryService,useValue:repository}]}).compileComponents();});it('maps one server row per obligation and preserves unavailable-versus-zero display',async()=>{const fixture=TestBed.createComponent(PaymentsComponent);await fixture.componentInstance.ngOnInit();fixture.detectChanges();expect(fixture.componentInstance.rows().length).toBe(1);expect(fixture.nativeElement.textContent).toContain('$200.00');expect(fixture.nativeElement.textContent).toContain('Check');});it('sends search/filter/reset state to the server',async()=>{const component=TestBed.createComponent(PaymentsComponent).componentInstance;await component.onSearchChange('Avery');await component.onFilterChange({key:'kind',value:'deposit'});expect(repository.listObligations).toHaveBeenCalledWith(jasmine.objectContaining({search:'Avery',kind:'deposit'}));await component.reset();expect(repository.listObligations).toHaveBeenCalledWith(jasmine.objectContaining({search:'',kind:undefined}));});});
