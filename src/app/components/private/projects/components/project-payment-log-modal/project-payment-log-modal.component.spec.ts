import { TestBed } from '@angular/core/testing';import { ProjectPaymentLogModalComponent } from './project-payment-log-modal.component';
describe('ProjectPaymentLogModalComponent',()=>{let component:ProjectPaymentLogModalComponent;beforeEach(()=>{TestBed.configureTestingModule({imports:[ProjectPaymentLogModalComponent]});component=TestBed.createComponent(ProjectPaymentLogModalComponent).componentInstance;component.open=true;component.obligations=[{project_payment_record_id:'deposit',project_id:'project',payment_kind:'deposit',status:'due',amount_due:300,amount_paid:0,target_amount:300,outstanding_amount:300,payment_source:'manual',created_at:'2026-01-01',updated_at:'2026-01-01'}];component.ngOnChanges();});it('selects the fixed outstanding installment and emits valid receipt evidence',()=>{const values:any[]=[];component.confirm.subscribe(value=>values.push(value));component.received_at='2026-01-15';component.payment_method='check';component.submit();expect(values[0]).toEqual(jasmine.objectContaining({obligation_id:'deposit',amount:300,payment_method:'check'}));});it('rejects zero, future dates, and invalid methods',()=>{component.amount=0;component.submit();expect(component.error).toContain('greater than zero');component.amount=10;component.received_at='2999-01-01';component.submit();expect(component.error).toContain('not in the future');component.received_at='2026-01-01';component.payment_method='bad' as any;component.submit();expect(component.error).toContain('valid payment method');});it('emits duplicate and overpayment confirmations for server review',()=>{const values:any[]=[];component.confirm.subscribe(value=>values.push(value));component.received_at='2026-01-01';component.suspected_reference='BBP-2026-1';component.duplicate_override_reason='Verified separate check';component.confirm_overpayment=true;component.submit();expect(values[0]).toEqual(jasmine.objectContaining({suspected_reference:'BBP-2026-1',duplicate_override_reason:'Verified separate check',confirm_overpayment:true}));});});
